#include "main.h"
/**
 * _isupper - checks for an uppercase character.
 * @c: char that the code checks
 * Return: 1 if c is uppercase, otherwise 0
 */
int _isupper(int c)
{
if (_isupper(c))
{
_putchar(c);
}
return (0);
}
